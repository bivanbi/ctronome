const char *default_dsp_device_path = "/dev/dsp\0";

/* means: 1/4, 1/8 etc. */
const int default_base_note = 4;

const int default_beat_per_minute[2] = {60, 4}; /* 60 bpm is given for 1/4 notes */
const int default_beat_per_tact[2] = {1, 4}; /* beat per tact */
