dword get_next_line(byte *, FILE *, dword); /* read next line from file */
int search_byte_in_buffer(byte *haystack, byte needle); /* search one character and return its position. */
FILE *open_file_for_reading(byte *); /* open file for reading */
